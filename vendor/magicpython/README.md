# MagicPython (vendored)

`MagicPython.tmLanguage` and `LICENSE` are vendored unmodified from
[MagicStack/MagicPython](https://github.com/MagicStack/MagicPython)
(MIT License), used to highlight the raw Python inside a PRL rule's
`then ... end` block (scope `source.python`).
